<resources>

    <!-- Tema Pantalla Completa -->
    <style name="FullScreen" parent="@style/Theme.AppCompat.Light">
        <item name="windowActionBar">false</item> <!-- For 2.x version  -->
    </style>

    <!-- Tema Barra de Acciones -->
    <style name="TemaMiuzicappActionBar" parent="@style/Theme.AppCompat.Light">

    </style>

    <!-- Facebook Style -->
    <style name="FacebookLoginButton">
        <item name="android:textSize">@dimen/smallTxtSize</item>
        <item name="android:background">@color/color_facebook</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:layout_gravity">center_horizontal</item>
    </style>

</resources>
